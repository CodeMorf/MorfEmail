/**
 * DiscoveryProvider Interface - MorfEmail Discovery Layer
 * Abstracción para desacoplar el descubrimiento de URLs de proveedores específicos.
 */

import { DiscoveryProvider, DiscoveryQuery, DiscoveryResult } from '../types';

export type { DiscoveryProvider, DiscoveryQuery, DiscoveryResult };
